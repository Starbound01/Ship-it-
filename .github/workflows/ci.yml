name: CI

on:
  push:
  pull_request:

jobs:
  artifacts-check:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run artifacts check
        run: scripts\check_artifacts.cmd
